# ComfyUI_SaveImageWithoutMetadata
Save image like the native node but without metadata, supporting filename_prefix with variables like %date:yyyy-MM-dd% and %KSampler.seed% to include values from nodes
